// Served by the `kromaIconCatalog` plugin in @kroma/ui/vite; no file on disk.

declare module 'virtual:kroma-icon-catalog' {
  import type { IconCatalog } from '@kroma/ui/kit';

  export const ICON_CATALOG: IconCatalog;
}
