{
  "name": "@kroma/stats-relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "KROMA statistics collector: the Cloudflare Worker at stats.kroma.tv that counts running installs from the opt-in heartbeat each one sends, and serves the aggregate the public page reads.",
  "author": "Maxime Scharwath <maxscharwath@gmail.com>",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxscharwath/kroma.git",
    "directory": "packages/stats-relay"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute kroma-stats --remote --file worker/schema.sql"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "hono": "^4.13.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.123.0"
  }
}
