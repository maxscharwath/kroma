import { defineStory } from '@kroma/workbench/story';
import { Button } from '#ui/components/atoms/button';
import { EmptyState } from './empty-state';

export const story = defineStory({
  group: 'Feedback',
  component: EmptyState.Root,
  args: {
    icon: 'mood-empty',
    size: 'md',
    layout: 'inline',
  },
  controls: { icon: 'icon', size: ['sm', 'md', 'tv'], layout: ['inline', 'fill'] },
  render: (props) => (
    <EmptyState.Root {...props}>
      <EmptyState.Title>No results</EmptyState.Title>
      <EmptyState.Hint>Try another term, or check that the server is reachable.</EmptyState.Hint>
    </EmptyState.Root>
  ),
});

The empty screen: a glyph in its well, what is missing, and why, in the same voice as the 404 and 500 pages. Two axes, and they are independent: `size` is how loud it reads (a list inside a section, a page, a page at ten feet), `layout` is where it sits (under what came before, or centred in the whole region it was given).

```tsx
<EmptyState.Root icon="mood-empty">
  <EmptyState.Title>No results</EmptyState.Title>
  <EmptyState.Hint>Try another term.</EmptyState.Hint>
</EmptyState.Root>

<EmptyState.Root size="tv" layout="fill">
  <EmptyState.Media name="mood-empty" />
  <EmptyState.Title>No results</EmptyState.Title>
  <EmptyState.Actions><Button label="Retry" onPress={retry} /></EmptyState.Actions>
</EmptyState.Root>
```

<Scene name="With action">
  {story.take((props) => (
    <EmptyState.Root {...props}>
      <EmptyState.Title>No results</EmptyState.Title>
      <EmptyState.Hint>
        Try another term, or check that the server is reachable.
      </EmptyState.Hint>
      <EmptyState.Actions>
        <Button label="Retry" size="sm" />
      </EmptyState.Actions>
    </EmptyState.Root>
  ))}
</Scene>

<Scene name="Inside a section" args={{ size: 'sm' }}>
  The state stands in for a LIST under a heading, not for the screen: a smaller badge, a title sized to a section, and it sits straight under whatever introduced it.
</Scene>

<Scene name="A failed page">
  The module pages: the state fills the region and centres, the hint says what happened, the chip carries the raw cause, and retry is the one action. The same anatomy as the router error pages, minus the status number.

  <EmptyState.Root layout="fill" icon="alert-triangle">
    <EmptyState.Title>This page could not be loaded</EmptyState.Title>
    <EmptyState.Hint>The module is installed; its data did not answer.</EmptyState.Hint>
    <EmptyState.Detail>GET /api/module/tv.kroma.remote/remote 502</EmptyState.Detail>
    <EmptyState.Actions>
      <Button variant="glass" size="sm" icon="refresh" label="Retry" />
    </EmptyState.Actions>
  </EmptyState.Root>
</Scene>

<Scene name="Media of its own">
  A mark the kit cannot size goes through `<EmptyState.Media>` rather than `icon`.

  {story.take(({ size }) => (
    <EmptyState.Root size={size}>
      <EmptyState.Media name="mood-empty" />
      <EmptyState.Title>No results</EmptyState.Title>
      <EmptyState.Hint>Try another term.</EmptyState.Hint>
      <EmptyState.Actions>
        <Button variant="glass" size="sm" icon="refresh" label="Retry" />
      </EmptyState.Actions>
    </EmptyState.Root>
  ))}
</Scene>

<Do>
- Reach for `layout="fill"` only when the state IS the page, a whole screen with nothing on it.
- Put the raw cause in `<EmptyState.Detail>`: a status line, a path, an error message.
- Keep the mark on `icon`: it is an affordance, and `<EmptyState.Media>` is for media the kit cannot size.
</Do>

<Dont>
- Use `size="md"` for a list inside a section; a second page-scale headline halfway down a screen reads as a heading.
- Write more than one action; the state offers the one thing to do.
</Dont>
