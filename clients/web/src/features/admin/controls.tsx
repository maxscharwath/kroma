// Shim: these controls now live in `@luma/admin-kit`. Re-exported so existing
// call sites keep importing from `#web/features/admin/controls`.
export { Button, Disclosure, NumberField, SegmentedControl } from '@luma/admin-kit';
