// The GitHub repo whose latest Release carries `modules.json`. A named export
// beside the Hono app would trip workerd ("not of type ExportedHandler"), so
// shared constants live here.
export const DEFAULT_REPO = 'maxscharwath/kroma';
