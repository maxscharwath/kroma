{
  "name": "@kroma/registry",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The KROMA module registry contract (RFC 110): the zod schemas every document is parsed through, the builders that emit them, a typed client any conforming registry answers, and the JSON Schema derived from all three.",
  "author": "Maxime Scharwath <maxscharwath@gmail.com>",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxscharwath/kroma.git",
    "directory": "packages/registry"
  },
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
