[package]
name = "luma-config"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Resolved server configuration (env-parsed, zero I/O beyond env)"
license.workspace = true

[dependencies]
tracing = { workspace = true }
