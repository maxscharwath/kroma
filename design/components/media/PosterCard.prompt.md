The poster tile for rails, grids and search. Pass two `colors` for the key-art gradient; `progress` (0–100) draws the resume bar.

\`\`\`jsx
<PosterCard title="Sillage" genre="Thriller SF" badge="4K" colors={['#3A2E5C','#0E1430']} progress={38} />
\`\`\`
