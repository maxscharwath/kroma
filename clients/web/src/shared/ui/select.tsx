// The app-wide dropdown is the design system's <Select>: a dialog under a
// D-pad, an anchored listbox popover under a pointer, keyboard and ARIA built
// in. Re-exported so pages keep importing from `#web/shared/ui`.

export type { SelectOption, SelectProps } from '@kroma/ui/kit';
export { Select } from '@kroma/ui/kit';
