{
  "name": "@kroma/ci-tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The pipeline's CLI: every step a workflow runs is `bun run ci <command>`, so a job is a list of commands that also run on a laptop. Lanes, test shards, Rust coverage, fleet builds, the version stamp, the canary channel and cache hygiene.",
  "author": "Maxime Scharwath <maxscharwath@gmail.com>",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maxscharwath/kroma.git",
    "directory": "packages/ci-tools"
  },
  "exports": {
    ".": "./src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
