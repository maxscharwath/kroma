// Shim: the admin page header + primary action button now live in
// `@luma/admin-kit`. Re-exported so existing call sites keep importing from
// `#web/features/admin/header`.
export { HeaderAction, PageHeader } from '@luma/admin-kit';
