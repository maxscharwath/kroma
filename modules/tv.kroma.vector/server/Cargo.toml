# This module is its own cargo workspace: it builds standalone, outside the
# server tree, and is packed into a `.kmod` on its own. Package metadata and
# dependency versions are explicit here rather than inherited.
[workspace]

[package]
name = "kroma-vector"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
description = "Content embeddings (lexical default + optional MiniLM/candle semantic backend)"
license = "MIT"

[[bin]]
# The module's `.kmod` entrypoint: a port-provider-only sidecar serving the
# embedder over HTTP (see src/bin/module.rs).
name = "kroma-module-vector"
path = "src/bin/module.rs"

[features]
# Off by default: the dependency-free lexical embedder is the default backend.
# `semantic` pulls in the MiniLM sentence-transformer via candle.
semantic = ["dep:candle-core", "dep:candle-nn", "dep:candle-transformers", "dep:tokenizers"]

[dependencies]
kroma-module-sdk = { path = "../../../server/crates/kroma-module-sdk" }
# The `.kmod` binary: the out-of-process runtime + axum for the embedder routes.
kroma-module-runtime = { path = "../../../server/crates/kroma-module-runtime" }
axum = { version = "0.8", features = ["macros", "ws"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
candle-core = { version = "0.11", default-features = false, optional = true }
candle-nn = { version = "0.11", default-features = false, optional = true }
candle-transformers = { version = "0.11", default-features = false, optional = true }
tokenizers = { version = "0.23", default-features = false, features = ["onig"], optional = true }

# Auto-enable Metal on Apple-Silicon dev builds (see kroma-whisper for the
# rationale); inert until `semantic` pulls candle in.
[target.'cfg(target_os = "macos")'.dependencies]
candle-core = { version = "0.11", default-features = false, features = ["metal"], optional = true }
candle-nn = { version = "0.11", default-features = false, features = ["metal"], optional = true }
candle-transformers = { version = "0.11", default-features = false, features = ["metal"], optional = true }

[dev-dependencies]
# `oneshot`, to drive the embedder routes without binding a port.
tower = "0.5"

# The profile `bun run modules:pack` builds this sidecar with: release + `panic =
# "abort"`. Dropping the unwinding tables makes the binary ~11% smaller at NO
# speed cost (opt-level stays 3). Safe for a sidecar: a panic aborts the module
# process and the supervisor respawns it. Defined per module because a profile is
# only honoured at a workspace root, and each module is now its own workspace.
[profile.release-kmod]
inherits = "release"
panic = "abort"
