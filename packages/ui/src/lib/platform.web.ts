// The browser half of the platform constant; see platform.ts for the whole
// story of why this is a twin pair rather than a `Platform.OS` read.

/** True on the browser targets; this file IS the web resolution. */
export const WEB = true;
