import { styles } from '@kroma/ui/kit';

/** The two weights a television label takes beside the kit's own. */
export const weight = styles({ medium: { fontWeight: '500' }, semibold: { fontWeight: '600' } });
