# TV UI kit
10-foot LUMA: focusable top nav, hero that follows the focused rail item, amber focus ring + breathing glow, remote hints. See `index.html`. Reference app: `/LUMA.dc.html` (platform = TV).