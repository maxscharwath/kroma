// The amber restated as coral; everything else stays KROMA.

import { createTheme } from '@kroma/ui/kit';

export const ember = createTheme({
  colors: {
    accent: '#FF6B4A',
    accentHover: '#FF8A6E',
    accentBright: '#FF9C82',
    accentWash: '#FD6947',
    accentSoft: 'rgba(253, 105, 71, 0.16)',
    accentSoftHover: 'rgba(253, 105, 71, 0.26)',
  },
});
