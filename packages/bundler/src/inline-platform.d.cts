/** The source with the kit's platform import collapsed to a literal, or the
 * source untouched when it does not import one. See inline-platform.cjs. */
export function inlinePlatformConst(src: string, platform: string): string;
