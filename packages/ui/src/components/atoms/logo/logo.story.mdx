import { defineStory } from '@kroma/workbench/story';
import { Logo } from './logo';

export const story = defineStory({
  group: 'Brand',
  component: Logo,
  args: { size: 48, markOnly: false, spin: 'none' },
  controls: { size: { min: 16, max: 160, step: 8 }, spin: ['none', 'once', 'loop'] },
});

The KROMA lockup, color wheel included. This is the only copy of the brand's vector paths in the whole repository: everything that displays the logo goes through here.
