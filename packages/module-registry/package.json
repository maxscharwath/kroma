{
  "name": "@kroma/module-registry",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "KROMA module registry: the Cloudflare Worker at modules.kroma.tv that serves the modules.json catalog live from GitHub Releases, plus the React site that browses it (built to dist/, served as Worker static assets with the catalog injected at the edge).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && wrangler deploy -c worker/wrangler.jsonc"
  },
  "dependencies": {
    "hono": "^4.12.32",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "^6.0.3",
    "tailwindcss": "^4",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "wrangler": "^4.42.0"
  }
}
