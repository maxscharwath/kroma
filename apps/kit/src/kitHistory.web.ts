// `virtual:kroma-history` is served by the `gitHistory` plugin in
// @kroma/bundler, which reads the log once at build time. The Metro half of the
// pair is kitHistory.ts.

export { HISTORY } from 'virtual:kroma-history';
