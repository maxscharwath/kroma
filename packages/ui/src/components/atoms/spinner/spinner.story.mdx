import { defineStory } from '@kroma/workbench/story';
import { Spinner } from './spinner';

export const story = defineStory({
  group: 'Feedback',
  component: Spinner,
  args: { size: 28, thickness: 3 },
  controls: { size: { min: 16, max: 96, step: 4 }, thickness: { min: 1, max: 8, step: 1 } },
});

The indeterminate waiting indicator. It runs on `Animated` rather than CSS keyframes, which makes it correct on all four targets and immune to the animation freeze of a hidden window.
