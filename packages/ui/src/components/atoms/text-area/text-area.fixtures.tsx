import type { ControlSize } from '#ui/lib/field-shell';

export const SIZES: ControlSize[] = ['sm', 'md', 'tv'];

export const LONG =
  'The library scan finished at 04:12 and found 214 new files. Eleven of them have no year in the name, so they were left unmatched rather than guessed at, and they are waiting in the review queue. The rest went into Films. Nothing was moved on disk and nothing was deleted, so a second pass over the same folders is safe to run whenever you like. The two drives that were offline last week answered this time, which is where most of the new files came from.';
