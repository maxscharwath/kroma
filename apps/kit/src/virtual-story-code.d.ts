// Served by the `storyCode` plugin in @kroma/bundler; no file on disk.

declare module 'virtual:kroma-story-code' {
  import type { StoryCodes } from '@kroma/workbench';

  export const STORY_CODE: StoryCodes;
}
