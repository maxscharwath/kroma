import { defineStory } from '@kroma/workbench/story';
import { Box } from '#ui/components/atoms/box';
import { Ground } from '#ui/components/atoms/ground';
import { stillArt } from '#ui/lib/sample-art';
import { PostPlay } from './post-play';

export const story = defineStory({
  group: 'Player',
  // It fills whatever it is given, so the story varies the stage rather than
  // the component: a television first, since that is where it is read from
  // across a room.
  width: { min: 640, max: 1280 },
  viewport: 'tv',
  args: {
    stageWidth: 1920,
    title: 'Blade Runner',
    subtitle: '1982 · 1h57 · H.265 · 4K',
    overview:
      "Dans un Los Angeles noyé de pluie, un ancien policier traque quatre répliquants revenus sur Terre pour arracher à leur créateur les quelques années qu'il leur reste.",
    focus: 'play',
  },
  controls: {
    focus: { options: ['play', 'home'] },
    stageWidth: { min: 900, max: 1920, step: 20 },
  },
  render: ({ title, subtitle, overview, ...props }) => (
    <Ground tone="dark">
      <Box aspect={16 / 9} minH={420} bg="surface2" radius="lg" overflow="hidden">
        <PostPlay
          {...props}
          item={{ id: 'br', title, subtitle, overview, artUrl: stillArt(3) }}
          onPlay={() => {}}
          onHome={() => {}}
        />
      </Box>
    </Ground>
  ),
});

The screen a film ends on. It replaces the chrome rather than covering it: there is no playhead left to scrub and no transport worth showing, so what is left is the next film to watch and the way out.

`focus` is the player's, not the screen's. Both buttons are controlled, because the chrome runs its own virtual focus and a second, real one would let a television's focus engine light a button this machine is not on.

`stageWidth` picks the type tier rather than scaling one: a stage 1600 wide or more is a television and takes the kit's 10-foot ladder, anything narrower is a desk and takes the desktop one. The layout does not otherwise change.

```tsx
<PostPlay
  item={offer}
  focus={postPlayFocus}
  stageWidth={stageSize.width}
  onPlay={playOffer}
  onHome={goHome}
/>
```

<Scene name="Home focused" args={{ focus: 'home' }}>
  The second action. Back reaches it from anywhere on this screen.
</Scene>

<Scene name="On a desk, not a television" args={{ stageWidth: 1280 }}>
  The same layout under the desktop type tier.
</Scene>

<Scene name="Nothing written about it" args={{ overview: undefined }}>
  A title the metadata provider has no synopsis for. The block shortens; nothing
  else moves.
</Scene>

<Do>
- Offer something. With nothing to offer, leave the player instead of drawing
  this screen empty.
- Keep the copy inside the left half: the art is what the right half is for.
</Do>

<Dont>
- Start the offered film on a timer. A two-hour film nobody chose is not a
  default.
</Dont>
