// Which prop carries the outline WEIGHT, web (Tizen, webOS, desktop, browser).
//
// @tabler/icons-react spells it `stroke`, and applies it as the width. See the
// native sibling for why this is split at all.

const STROKE_PROP = 'stroke';

export { STROKE_PROP };
